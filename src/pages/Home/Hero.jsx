import AppCarousel from "@/components/reusable/AppCarousel";

const Hero = () => {
  return (
    <div className="w-full h-fit lg-light border">
      <AppCarousel />
    </div>
  );
};

export default Hero;
