import AppCarousel from "@/components/reusable/AppCarousel";

const Hero = () => {
  return (
    <div className="w-screen h-fit lg-light border">
      <AppCarousel />
    </div>
  );
};

export default Hero;
